## HelloChusquis v1.2.0

### What's new
- Fixed multi-step plan execution (tools now persist across all steps)
- Fixed web search (switched to reliable DuckDuckGo lite)
- Added `hellochusquis config` command to edit settings after setup
- Silent provider fallback — no more error messages when a provider hits rate limits
- Demo GIF added to README

### Install
pip install hellochusquis