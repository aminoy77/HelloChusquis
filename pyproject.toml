[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hellochusquis"
version = "1.5.1"
description = "A powerful terminal AI agent with 150+ integrations, 100+ functions, voice, web UI, and auto-tool-builder"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "aminoy77"}
]
keywords = ["ai", "agent", "cli", "llm", "automation", "mcp", "tools"]
requires-python = ">=3.10"
dependencies = [
    "rich>=13.0.0",
    "pyyaml>=6.0",
    "httpx>=0.24.0",
    "fastapi>=0.100.0",
    "uvicorn>=0.23.0",
]

[project.urls]
Homepage = "https://github.com/aminoy77/HelloChusquis"

[project.scripts]
hellochusquis = "cli:main"

[tool.setuptools.packages.find]
where = ["."]

[tool.setuptools.package-data]
"web" = ["*.html"]
